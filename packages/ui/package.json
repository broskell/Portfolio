{
  "name": "@portfolio/ui",
  "version": "1.0.0",
  "private": true,
  "description": "Shared low-level UI primitives for the portfolio and dashboard when reuse is justified.",
  "type": "module",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=20",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "node -e \"console.log('UI package has no generated build output to clean yet.')\""
  }
}
