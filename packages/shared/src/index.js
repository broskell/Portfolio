// Shared exports belong here so the portfolio, dashboard, and server can agree on
// names, route constants, validation schemas, and DTO shapes as the CMS grows.
export const WORKSPACE_PACKAGE = '@portfolio/shared'

