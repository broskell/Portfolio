{
  "name": "@portfolio/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared schemas, constants, DTOs, and route contracts for the portfolio ecosystem.",
  "type": "module",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=20",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "node -e \"console.log('Shared package has no generated build output to clean yet.')\""
  }
}
