// The API client package will centralize HTTP behavior so both React apps handle
// base URLs, credentials, retries, and response envelopes the same way.
export const WORKSPACE_PACKAGE = '@portfolio/api-client'

