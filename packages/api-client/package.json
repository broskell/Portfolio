{
  "name": "@portfolio/api-client",
  "version": "1.0.0",
  "private": true,
  "description": "Shared API client package consumed by the public portfolio and admin dashboard.",
  "type": "module",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=20",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "node -e \"console.log('API client package has no generated build output to clean yet.')\""
  }
}
